<div class="u-info">
  <%= link_to gravatar_for(current_user,size: 50), current_user %>
  <h1><%= current_user.name %></h1>
  <span><%= pluralize current_user.microposts.count, t("microposts") %></span><br>
  <span><%= link_to t("micropost.view"), current_user %></span>
</div>

